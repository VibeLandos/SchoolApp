# План

## Сейчас

Рабочий каркас дневника: страница как в бумажном дневнике (номер, предмет, д/з в строке), добавление через нижние шиты, локальная база Room. Оценок нет и не планируются. Язык интерфейса: русский по умолчанию, английский из меню.

## Сделано

- Пустой Android-проект превращён в Compose-приложение
- Модель: уроки повторяются каждую неделю; домашка привязана к дате и номеру урока
- UI «тетрадь в обложке»: дни пн–сб, только вписанные уроки, кнопка «добавить урок»
- Вкладка всех заданий с фильтром и отметкой «сделано»
- К д/з можно прикрепить несколько фото (галерея, до 8)
- Шиты короче: урок без полей начала/конца (время из общего расписания звонков), кабинет по желанию
- Свайп между днями **текущей** недели; длина недели 5 (пн–пт) или 6 (пн–сб) из меню
- Английский интерфейс из бокового меню (русский по умолчанию, не от языка системы); строки в `strings.xml`
- Звонки: быстрая настройка (длина урока, обычная перемена, другие перемены) на неделю, день можно задать отдельно; вкладка «Сейчас» — до конца урока, перемены и дня
- Расписание дня или недели отправляется текстом через выбор приложения и вставляется обратно (без д/з)
- Двойное нажатие «Дневник» в нижнем меню открывает сегодняшний день
- Вкладка «Сейчас» обновляет дату в полночь, если экран оставлен открытым
- Превью фото декодируется в IO; фото д/з на странице группируются один раз, не фильтром в каждой строке
- Debug-сборка ставится и открывается на двух телефонах (Redmi Note 10S / POCO F5): `MainActivity` в Resumed, без FATAL в logcat
- Пейджер не пересоздаётся при смене недели (`key(weekDates)` убран); на странице дня нет `verticalScroll`, чтобы не спорить со свайпом
- Strong Skipping у Compose Compiler; release с R8 — для проверки реальной скорости, не debug JIT. Подпись v1/v2/v3, чтобы APK ставился с файла; свой keystore опционально (`signing/keystore.properties`)
- Боковое меню с оформлением: обложка / система (обои слегка красят кожу, страница остаётся тетрадью) / ночь (темнее обложка, приглушённая бумага). Закрытие: свайп и жест назад; с края меню не открывается, чтобы не спорить с днями.
- Свайп на 120 Гц: пейджер без `beyondBoundsPageCount`, строки без `weight` (фиксированная колонка предмета), один жест на строку без ripple, мета урока не собирается заново каждый кадр.

## Ближайшее

Крупных пунктов в очереди нет. Не начинать идеи, пока нет явной просьбы «делай».

## Будущее (заказано)

- **Тёмная тема** — сделано: ночью та же тетрадь, темнее кожа, бумага не инвертируется.
- **Режим Material You** — сделано: обои слегка тонируют обложку, сетка уроков остаётся бумажной.
- **Боковое меню** — сделано для оформления, языка и звонков.
- **Английский язык** — сделано: строки в `res/values` (русский по умолчанию) и `res/values-en`; переключатель в боковом меню. Записанные предметы и д/з в базе не переводятся.
- **Звонки один раз, не на каждый урок** — сделано: быстрая настройка на неделю, своё расписание на день (суббота). Страница дня берёт время из расписания этого дня.
- **Сейчас** — сделано: третья вкладка; на уроке — до конца урока, на перемене — до конца перемены, плюс до конца дня. Список только оставшихся уроков. Нужны вписанные уроки на сегодня.
- В шите урока поля начала/конца уже убраны; время только из расписания звонков.
- **Импорт и экспорт расписания текстом** — сделано: в меню «Отправить день / неделю» (системный выбор приложения, text/plain) и «Вставить расписание». Домашка не входит. День применяется к открытой странице, неделя — ко всему расписанию и длине недели.
- **«Сейчас» после полуночи** — сделано: секундный тикер обновляет и время, и `LocalDate.now()`.
- **Пустой день и кнопка «добавить урок»** — сделано: сетка не держит 8 пустых строк. Номер урока выбирается в шите (включая 0). Звонки 1–8 как раньше; 0 — слот перед первым, дальше 8 — продолжение.
- **Рабочая неделя** — сделано: в меню 5 дней (пн–пт) или 6 (пн–сб). Суббота при пятидневке — выходной, как воскресенье.
- **Автообновление с GitHub** — не начинать без «делай»: проверка новой сборки в Releases репозитория и установка APK с телефона, без магазина. Аккаунт не нужен.

## Идеи (не делать, пока не выбраны)

Имеет смысл именно для дневника, без журнала оценок:

- Уведомление «урок скоро кончится» / «осталось N минут до конца дня» — пара к экрану звонков.
- Виджет на рабочий стол: следующий урок + д/з на сегодня.
- Сфотографировать задание с камеры, не только из галереи.
- Свои названия предметов и цвета строк.
- Каникулы / праздники: дни без сетки уроков.
- Поиск по тексту д/з.
- Локальный экспорт/бэкап (файл), без аккаунта — отдельно от текстовой отправки расписания.
- Напоминание вечером: что не сделано на завтра.
- Поворот/обрезка фото.
- Буфер «отмена» после случайного стирания строки.

Слабее стыкуется с «как бумажный дневник», лучше не в первую очередь: чаты, облако, аккаунты, оценки, ИИ.

## Не делаем, пока нет в этом файле

Оценки, аккаунты, сеть, многомодульность.
